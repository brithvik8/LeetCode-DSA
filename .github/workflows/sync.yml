name: Sync Leetcode
on:
  workflow_dispatch:
  schedule:
    - cron: "30 16 * * *" # Runs automatically every day at 10:00 PM IST
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Sync
        uses: joshcai/leetcode-sync@v1.7 # Updated to latest stable version
        with:
          github-token: ${{ github.token }}
          leetcode-csrf-token: ${{ secrets.LEETCODE_CSRF_TOKEN }}
          leetcode-session: ${{ secrets.LEETCODE_SESSION }}
          destination-folder: LeetCode-Solutions
          verbose: true # <-- THIS FORCES LEETCODE NUMBERS AND PERCENTILES
